Given a string as input, print all its permutations in sorted order.
Input- The only liine in input contain a string S.
Output- Print all the permutations of the string, one in each line, in recursive replacement order.
